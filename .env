# ===== 站点地址（用于 SEO canonical / Open Graph / 结构化数据）=====
# 部署后替换为你的真实域名或 Pages 地址（不带末尾斜杠）
# GitHub Pages 示例：https://yourname.github.io/portfolio
# 自定义域名示例：https://www.your-domain.com
VITE_SITE_URL=https://yourname.github.io/portfolio

# ===== 接口地址配置（mock / 真实后端切换入口）=====
# 默认 mock 模式：读取站点内 ./mock/portfolio.json，无需后端，更新内容只需改 JSON
# 对接真实后端/CMS 时改为，例如：
#   VITE_API_BASE_URL=https://api.your-domain.com
#   VITE_API_PORTFOLIO_ENDPOINT=/portfolio
VITE_API_BASE_URL=./mock
VITE_API_PORTFOLIO_ENDPOINT=/portfolio.json
