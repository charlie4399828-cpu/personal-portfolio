# 主站生产构建配置（GitHub Pages）
# 默认读取仓库内置的 public/mock/portfolio.json，无需后端即可完整访问。
# 若在仓库 Settings → Secrets 中配置了 VITE_API_BASE_URL（如 Render 后端地址），
# GitHub Actions 会生成 .env.production.local 覆盖以下两项，主站即连接真实后端。
VITE_API_BASE_URL=./mock
VITE_API_PORTFOLIO_ENDPOINT=/portfolio.json
