# 管理后台生产构建配置（部署在 GitHub Pages 的 /admin/ 子路径）
# 默认 mock 模式：可用 admin / admin123 登录预览，数据不落库（页头有 mock 标识）。
# 配置 GitHub Actions secret VITE_API_BASE_URL（Render 后端地址）后自动切换为真实后端。
VITE_API_BASE_URL=/mock/api
